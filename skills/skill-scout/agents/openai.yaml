interface:
  display_name: "Skill Scout"
  short_description: "Find skills, MCP servers, and plugins before a task"
  default_prompt: "Use $skill-scout before this task to find relevant skills, MCP servers, and plugins, review candidates, and recommend useful additions."
policy:
  allow_implicit_invocation: true
